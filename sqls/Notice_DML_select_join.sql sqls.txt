
use db_notice;

SELECT
N.CONTENT,
W.WRITER,
COUNT(*) AS VISTOR_COUNT
FROM
NOTICE N
INNER JOIN
WRITER W
ON
N.WRITER_ID = W.WRITER_id
INNER JOIN
VISITOR V
ON
N.NOTICE_ID = V.NOTICE_ID
GROUP BY
N.NOTICE_ID;